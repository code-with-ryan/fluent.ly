{
  "name": "server",
  "version": "1.0.0",
  "description": "This server connects into a MongoDB cluster to perform CRUD operations on flashcards.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "code-with-ryan",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
